targets:
  $default:
    builders:
      freezed:
        options:
          generic_argument_factories: true
          make_collections_unmodifiable: false
      json_serializable:
        options:
          explicit_to_json: true
