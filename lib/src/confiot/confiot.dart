/// [ConfIoTFile] and [ConfIoTNamespace] (with its converters) moved to
/// `package:layrz_sdk`. This library is kept as an empty pass-through so
/// existing `import 'package:layrz_models/src/confiot/confiot.dart'` paths in
/// downstream code keep resolving; the types themselves are re-exported from
/// the main `layrz_models.dart` barrel via the SDK.
library;
